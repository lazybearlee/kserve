apiVersion: "serving.kserve.io/v1beta1"
kind: "InferenceService"
metadata:
  name: "sklearn-iris-baseline" # Unique name for this scenario
spec:
  predictor:
    # containerConcurrency: 8 # Optional: Keep or remove based on Knative/QP usage
    sklearn:
      storageUri: "gs://kfserving-examples/models/sklearn/1.0/model"
    # --- NO Batcher section ---
    # --- NO Cache section ---